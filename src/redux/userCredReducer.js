import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const userCredentials = createSlice({
  name: "userCred",
  initialState,
  reducers: {},
});

export const {} = userCredentials.actions;
export default userCredentials;
