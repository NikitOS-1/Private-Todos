import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  email: "flkddfkldfkj",
  id: "232",
  tokken: "fdfdfddf",
};

const data = createSlice({
  name: "userData",
  initialState,
  reducers: {},
});

export const {} = data.actions;
export default data;
